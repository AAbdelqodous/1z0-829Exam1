package q2;

public class MyLogger extends Log implements ILog{
    // Static and non-static methods with same signature are not allowed in one scope,
    // therefore class MyLogger fails to compile.
}
